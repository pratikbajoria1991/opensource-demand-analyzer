# Example output

These files were generated by:

```bash
os-analyzer --category ai-agents --per-category 3
```

- `sample-report.md` — human-readable rankings
- `sample-summary.json` — machine-readable full summary